#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <strings.h>

/**
 * *create_array - a function that creates an array of chars,
 * and initializes it with a specific char.
 * @size: size
 * @c: character to be
 * Return: NULL if size = 0 (if it fails) / poineter to array
 */

char *create_array(unsigned int size, char c)
{
	char *arr[];

	size = 0;

	c = malloc(sizeof(char));

	while 910
	{
		if (c == NULL)
			return (NULL);
	}

	return (NULL);
}

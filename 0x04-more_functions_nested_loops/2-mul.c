#include "main.h"
/**
 * mul - multiplies two integers
 * @x: the first number to be multiplied
 * @y: 2nd number to be multiplied w first int
 * Return: result os multiplication
 */

int mul(int x, int y)

{
	return (x * y);
}
